# Polar.DB: экспериментальное исследование компромиссов между индексированным чтением, мутациями и восстановлением состояния

> Черновик статьи. Экспериментальные данные обновлены по publication-ready серии `20260810T030729570Z` на commit `e093da0247ec58c7fb78fc381eca52fa002b0967`. Подробный протокол вынесен в `EXPERIMENT_METHOD.md`.

## Аннотация

Polar.DB — библиотека для хранения структурированных данных в .NET, ориентированная на append-потоки, бинарное представление записей и специализированные индексы. В работе исследуется не вопрос «какая СУБД быстрее вообще», а более узкий вопрос: какие характеристики даёт такой дизайн на различных классах операций по сравнению с SQLite при одинаковой логической нагрузке.

Экспериментальная серия включает 16 сценариев на наборах 50 тыс. и 5 млн записей: построение целочисленного primary index, point lookup по primary key четырёх типов, выборку по неуникальному external key четырёх типов, тяжёлую выборку по высокочастотному external key, append, logical delete и reopen. SQLite и Polar.DB выполнялись в отдельных процессах; результаты принимались только для Release-сборки с одинаковым commit и прошедшей проверкой ожидаемого количества строк и checksum.

На 5 млн записей Polar.DB показал преимущество на построении primary index (186,8 против 1488,9 мс), point lookup по primary key (примерно 1,65–3,72 раза), append/delete и размере файлов. SQLite оказался сильнее на массовой выдаче по secondary/external index и особенно на переходе из закрытого состояния в query-ready: 0,0133 мс против 701,7 мс в данном протоколе. Результаты показывают архитектурный компромисс: перенос части индексного состояния в оперативную память уменьшает стоимость steady-state point lookup, но увеличивает стоимость подготовки query-ready состояния после reopen.

## 1. Постановка задачи

Сравнение систем хранения часто сводится к одной агрегированной величине «операций в секунду». Для Polar.DB такое представление малоинформативно. Архитектура библиотеки разделяет физическую последовательность данных, primary index, external indexes и динамическое состояние после append. Разные операции проходят по разным путям и по-разному используют диск и оперативную память.

Поэтому исследовательский вопрос формулируется следующим образом:

**какие классы операций выигрывают и какие проигрывают при append-oriented хранении и частичной материализации индексного состояния в памяти по сравнению с SQLite на одной машине и одинаковых логических запросах?**

Работа не претендует на универсальное ранжирование Polar.DB и SQLite. SQLite является зрелой универсальной embedded SQL СУБД; Polar.DB — специализированной библиотекой с иными целями и моделью хранения. Сравнение используется как практическая контрольная точка для оценки конкретных инженерных свойств Polar.DB.

## 2. Исследуемая организация Polar.DB

Для сценариев статьи существенны четыре свойства.

Первое — исходные записи хранятся в append-oriented бинарной последовательности. Изменение логического состояния может добавлять новую физическую запись вместо произвольного перемещения существующих данных.

Второе — primary index отделён от основной последовательности. Для query-ready состояния актуальные данные primary index частично поднимаются в оперативную память. В текущей версии вместе с массивом hash/key-представления в памяти удерживаются и соответствующие физические offsets статического snapshot.

Третье — изменения после построения snapshot остаются динамическим слоем и имеют приоритет над статической частью. Это сохраняет last-write-wins semantics для replace и tombstone.

Четвёртое — external indexes предназначены для неуникальных ключей и должны возвращать множество записей. Поэтому их стоимость определяется не только поиском границы диапазона, но и объёмом materialization результата и проверкой актуальности найденных записей.

Такая организация позволяет ожидать разные результаты для point lookup, массовой выборки и reopen. Эксперименты ниже проверяют именно это различие.

## 3. Экспериментальная схема

Полная методика, параметры выборок и правила валидации приведены в `EXPERIMENT_METHOD.md`. Здесь перечислены только условия, необходимые для интерпретации результатов.

Основная серия:

- run id: `20260810T030729570Z`;
- Polar.DB commit: `e093da0247ec58c7fb78fc381eca52fa002b0967`;
- рабочее дерево: clean;
- конфигурация: Release;
- runtime: .NET 10.0.7;
- ОС, записанная benchmark manifest: Microsoft Windows 10.0.26200, x64;
- число логических процессоров, видимое процессу: 12;
- Polar.DB assembly: 2.1.3.0;
- Microsoft.Data.Sqlite assembly: 9.0.4.0;
- Server GC: выключен;
- Tiered Compilation, Tiered PGO и ReadyToRun: runtime defaults.

Точная модель CPU, объём и конфигурация RAM, модель накопителя и редакция Windows будут внесены после отдельного снимка аппаратной среды. Это важно для воспроизводимости IO- и memory-sensitive результатов.

SQLite и Polar.DB не выполняются внутри одного процесса. Coordinator запускает отдельный child process для каждого engine, проверяет commit и build settings, а затем объединяет raw results. Порядок engine чередуется детерминированно, чтобы один engine не получал постоянного преимущества от позиции в серии.

Основные наборы содержат 50 000 и 5 000 000 записей. В тексте ниже показаны главным образом 5 млн как более показательный размер; результаты 50 тыс. остаются в raw artifacts и используются как контроль масштаба.

## 4. Результаты

### 4.1. Построение primary index

В изолированном сценарии построения целочисленного primary index на 5 млн идентификаторов получены следующие median values:

| Engine | Build + flush, мс | Размер артефакта |
|---|---:|---:|
| Polar.DB | **186,8** | 100,0 MB |
| SQLite | 1488,9 | 118,6 MB |

В данном сценарии Polar.DB выполняет операцию примерно в 7,97 раза быстрее. Этот результат относится только к специально выделенной операции построения primary integer index и не должен переноситься на полную загрузку базы или построение произвольных индексов.

### 4.2. Point lookup по primary key

Для hot phase на 5 млн записей:

| Тип primary key | Polar.DB, мс/query | SQLite, мс/query | Отношение |
|---|---:|---:|---:|
| `int` | **0,00406** | 0,00794 | Polar.DB ≈ 1,95× |
| `long` | **0,00392** | 0,00847 | Polar.DB ≈ 2,16× |
| `Guid` | **0,00381** | 0,01416 | Polar.DB ≈ 3,72× |
| `string` | **0,00458** | 0,00755 | Polar.DB ≈ 1,65× |

В этой серии преимущество наблюдается для всех четырёх типов ключа. Для primary lookup размер Polar.DB artifacts составил около 575 MB против 1688,6 MB у SQLite, то есть примерно в 2,94 раза меньше.

Важно, что measured lookup не включает создание набора данных, build и reopen. Он характеризует уже подготовленное query-ready состояние.

### 4.3. Неуникальный external key

Обычный external-key dataset использует 1000 различных значений external key. При 5 млн записей один успешный запрос возвращает около 5000 строк.

Hot phase:

| Тип external key | Polar.DB, мс/query | SQLite, мс/query |
|---|---:|---:|
| `int` | 24,56 | **22,08** |
| `long` | 23,87 | **22,18** |
| `Guid` | 24,20 | **24,14** |
| `string` | 27,09 | **22,39** |

Здесь картина меняется. Для `Guid` системы практически равны, а для остальных типов SQLite быстрее примерно на 8–21%. Это показывает, что преимущество Polar.DB на point lookup нельзя автоматически переносить на materialization больших диапазонов secondary index.

Размер artifacts для обычного external lookup составил около 635 MB у Polar.DB и 1688,6 MB у SQLite.

### 4.4. Высокочастотный external key

Отдельная серия имитирует «famous key»: одно значение external key принадлежит 40% записей. На 5 млн строк успешный запрос возвращает 2 млн записей.

Median hot lookup:

| Тип external key | Polar.DB, мс/query | SQLite, мс/query |
|---|---:|---:|
| `int` | 5408 | **3654** |
| `long` | 5563 | **3580** |
| `Guid` | 5324 | **3721** |
| `string` | 5499 | **3643** |

SQLite быстрее примерно в 1,43–1,55 раза. Это наиболее явный результат для массовой secondary-index выдачи: когда стоимость поиска ключа становится малой по отношению к обходу и materialization миллионов строк, устройство result path оказывается важнее скорости точечного поиска.

### 4.5. Append и logical delete

Для мутаций разделены две метрики.

`Volatile mutation` измеряет операцию до persistence boundary. Она показывает стоимость изменения рабочего состояния, но не является мерой durable write latency.

`Durable mutation` — среднее время одной операции внутри batch из 100 изменений, в котором присутствует persistence boundary. Для SQLite это transaction commit, WAL checkpoint и file sync; для Polar.DB — Flush и file sync.

На 5 млн исходных записей:

| Операция | Polar.DB | SQLite | Комментарий |
|---|---:|---:|---|
| append, volatile | **0,0006 мс** | 0,1108 мс | ≈184,7× |
| append, durable | **0,310 мс** | 0,678 мс | ≈2,19× |
| delete, volatile | **0,0005 мс** | 0,0408 мс | ≈81,6× |
| delete, durable | **0,276 мс** | 0,354 мс | ≈1,28× |

Большие коэффициенты volatile path нельзя интерпретировать как преимущество durable storage на два порядка. После включения persistence boundary различие резко сокращается. Именно durable values следует использовать при обсуждении практической стоимости сохранения изменений.

### 4.6. Reopen: основная цена RAM-oriented query-ready состояния

Reopen измерялся двумя способами. `Open-only` учитывает только открытие/закрытие storage handles. `Query-ready reopen` включает открытие, подготовку metadata/index state и один indexed primary-key lookup.

На 5 млн записей:

| Engine | Open-only median, мс | Query-ready reopen median, мс |
|---|---:|---:|
| Polar.DB | 1,084 | 701,7 |
| SQLite | **0,00065** | **0,0133** |

Эта разница является центральным отрицательным результатом серии. Polar.DB подготавливает RAM-resident структуры, благодаря которым последующий primary point lookup выполняется быстро, но стоимость этой подготовки переносится на reopen. Для workload с частыми открытиями короткоживущих storage instances такая модель может быть невыгодна даже при очень быстрых steady-state lookup.

Поэтому корректная формулировка результата должна связывать обе метрики:

**в исследованной версии Polar.DB ускоряет steady-state point lookup ценой существенно более дорогого перехода в query-ready состояние после reopen.**

## 5. Изменение primary offset cache

Между publication-ready сериями `20260808T095444545Z` и `20260810T030729570Z` production-изменение в исследуемом primary lookup path заключается в хранении массива static offsets в памяти рядом с primary hash/key snapshot. Тестовые изменения не влияют на benchmark implementation.

Сравнение hot primary lookup на 5 млн записей:

| Ключ | До, мкс/query | После, мкс/query | Изменение |
|---|---:|---:|---:|
| `int` | 5,284 | **4,065** | −23,1% |
| `long` | 5,488 | **3,921** | −28,6% |
| `Guid` | 5,862 | **3,811** | −35,0% |
| `string` | 10,049 | **4,585** | −54,4% |

Одновременно median query-ready reopen вырос приблизительно с 564,5 до 701,7 мс (+24,3%).

Это сравнение поддерживает ожидаемый архитектурный trade-off, однако не является строгим isolated A/B experiment: две полные серии выполнялись в разное время. Поэтому проценты допустимо использовать как наблюдаемое изменение между версиями, но не как точную причинную оценку стоимости конкретной строки кода.

## 6. Обсуждение

Результаты не образуют линейного рейтинга двух систем.

Polar.DB особенно силён там, где workload совпадает с его организацией данных: построение специализированного primary index, подготовленный point lookup и append-oriented изменение состояния. Дополнительное хранение offsets в памяти ещё сильнее смещает профиль в сторону дешёвого steady-state чтения.

SQLite сильнее в двух важных классах задач. Во-первых, query-ready reopen почти не требует восстановления дополнительного RAM snapshot. Во-вторых, при выдаче больших диапазонов по неуникальному индексу его B-tree/row access path оказывается конкурентнее, а при миллионах возвращаемых строк — заметно быстрее.

Отсюда следует практический вывод: выбор Polar.DB нельзя обосновывать только низкой latency одного lookup. Полезность архитектуры зависит как минимум от отношения числа point queries к числу reopen, доли массовых secondary-index запросов и требований к объёму хранения.

Отдельно заметен footprint. Во всех основных lookup-сценариях файлы Polar.DB существенно меньше соответствующих SQLite artifacts. Это может быть самостоятельным преимуществом для embedded workloads, однако размер файлов не эквивалентен полному потреблению ресурсов: query-ready Polar.DB использует дополнительные RAM arrays, поэтому disk footprint и runtime memory следует рассматривать совместно.

## 7. Ограничения

Исследование имеет несколько ограничений.

1. Измерения проведены на одной Windows-машине. Результаты нельзя напрямую переносить на другие CPU, накопители, Linux или иной memory pressure.
2. Сравниваются конкретные реализации workload, а не все возможные способы настройки SQLite и Polar.DB.
3. В основной статье анализируется преимущественно размер 5 млн записей; размер 50 тыс. используется как контрольная точка и доступен в raw data.
4. Cold/hot в benchmark protocol означает состояние, определённое harness, а не гарантированно очищенный аппаратный page cache ОС.
5. Высокочастотный external-key сценарий намеренно экстремален: 40% набора имеет один ключ.
6. Версионное сравнение primary offset cache выполнено по двум отдельным полным сериям и не заменяет interleaved A/B experiment.
7. Текущая серия фиксирует wall-clock latency и resource snapshots; она не является microarchitectural profiling CPU cache misses, branch prediction или block-level IO.

## 8. Выводы

Эксперименты показывают, что для Polar.DB полезнее говорить не об общей «скорости базы», а о профиле стоимости операций.

На 5 млн записей текущая версия:

- существенно ускоряет построение специализированного primary index относительно выбранной SQLite реализации;
- выполняет hot primary-key point lookup примерно в 1,65–3,72 раза быстрее для исследованных `int`, `long`, `Guid` и `string`;
- показывает особенно дешёвый volatile append/delete и умеренное преимущество после включения persistence boundary;
- использует существенно меньший объём дисковых artifacts в исследованных lookup workloads;
- немного или заметно уступает SQLite при материализации тысяч и миллионов строк через неуникальный external index;
- платит высокой стоимостью query-ready reopen за RAM-resident индексное состояние.

Главный результат работы — не отдельный коэффициент ускорения, а выявленный компромисс **steady-state primary lookup ↔ query-ready reopen**. Именно этот компромисс должен определять дальнейшую оптимизацию Polar.DB и выбор workloads, где библиотека архитектурно оправдана.

## Артефакты воспроизводимости

Publication-ready raw series:

`benchmarks/results/raw/<experiment>/20260810T030729570Z/`

Для каждого эксперимента сохраняются:

- `combined.raw.json`;
- `manifest.json`;
- `samples.csv`;
- `sqlite.worker.json`;
- `polar-db.worker.json`.

Полная команда запуска:

```powershell
pwsh -File .\benchmarks\scripts\run-new-benchmarks.ps1
```

Подробное определение фаз, sampling plan, correctness checks и persistence boundaries приведено в `EXPERIMENT_METHOD.md`.
