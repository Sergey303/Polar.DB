# Экспериментальное исследование эффективности последовательностей и индексных построений библиотеки Polar.DB

УДК 004.65

> Единый рабочий черновик статьи. В него включены основной текст, подробная методика экспериментов и технические сведения о вычислительной среде. Численные результаты относятся к публикационной серии `20260810T030729570Z`, commit `e093da0247ec58c7fb78fc381eca52fa002b0967`, если явно не указано иное.

## Аннотация

В статье продолжается исследование библиотеки Polar.DB, предназначенной для создания специализированных средств структурирования, хранения и обработки данных. Рассматривается современная реализация последовательности объектов и индексных построений, а также экспериментально оцениваются характеристики основных операций: построение первичного индекса, поиск по первичному и внешнему ключу, добавление и логическое удаление записей, повторное открытие хранилища. В качестве внешней точки сравнения используется SQLite. Эксперименты выполнены на массивах из 50 тыс. и 5 млн записей; обе системы получают одинаковые логические задания, а правильность результата контролируется по числу возвращённых записей и контрольным суммам.

На массиве из 5 млн записей Polar.DB построил исследуемый целочисленный первичный индекс за 186,8 мс против 1488,9 мс у SQLite. В подготовленном состоянии поиск одной записи по первичному ключу для типов `int`, `long`, `Guid` и `string` оказался быстрее в 1,65–3,72 раза. При выборке по неуникальному внешнему ключу, когда один запрос возвращал около 5000 записей, SQLite был быстрее на 0–21%, а при высокочастотном ключе с результатом 2 млн записей — примерно в 1,43–1,55 раза. Для добавления и удаления обнаружена зависимость результата от размера исходного массива и от того, включена ли в измерение фиксация изменений на накопителе. Размер файлового представления Polar.DB в исследованных сценариях был существенно меньше. Эксперимент с повторным открытием выявил заметную стоимость подготовки первичного индекса Polar.DB в оперативной памяти. Первоначально полученные значения повторного открытия SQLite исключены из межсистемного сравнения после рецензирования методики, поскольку `Microsoft.Data.Sqlite` по умолчанию использует пул соединений, а в использованной версии теста он не отключался.

Полученные результаты показывают, что эффективность специализированного хранилища следует характеризовать не одной интегральной величиной, а набором характеристик для различных классов операций. Для Polar.DB наиболее заметным является сочетание быстрого поиска по первичному ключу в подготовленном состоянии, небольшого файлового представления и малой стоимости изменений до фиксации с дополнительными затратами на подготовку индексных структур и с меньшей эффективностью массовой выборки по неуникальному индексу.

**Ключевые слова:** Polar.DB, структурированные данные, база данных, последовательность, первичный ключ, индексное построение, внешний индекс, SQLite, экспериментальное исследование.

## 1. Введение

Развитие информационных систем сопровождается постоянным увеличением объёмов обрабатываемых структурированных данных и разнообразия схем их организации. Универсальные системы управления базами данных решают широкий класс задач, однако для ряда предметных областей существенное значение имеет возможность построения специализированных средств хранения, в которых представление данных и индексные построения непосредственно соответствуют структуре решаемой задачи.

Система PolarDB была предложена как средство создания специализированных баз данных и СУБД [1]. В дальнейшем была разработана библиотека PolarDB работы со структурированными данными, охватывающая типовые определения, сериализацию, отображение данных на байтовые потоки, индексные построения и другие задачи [2]. Следующим шагом стала модель базы данных, основанная на последовательности объектов, и универсальные средства индексирования последовательности [3]. В современной версии проекта используется написание **Polar.DB**; названия PolarDB в ссылках [1–3] сохранены в соответствии с опубликованными работами.

Ранее экспериментальные реализации Polar применялись, в частности, для хранения RDF и выполнения запросов SPARQL [4]. Настоящая работа продолжает это направление, но предметом эксперимента является не отдельная RDF-модель, а базовые операции универсального хранилища: построение индекса, поиск, изменение последовательности и восстановление состояния после повторного открытия.

### 1.1. Актуальность исследования

Актуальность работы определяется необходимостью оценивать системы хранения не только по функциональности, но и по стоимости основных операций при увеличении объёма данных. Это особенно существенно для специализированных хранилищ. Архитектурное решение, выгодное для одной операции, может приводить к дополнительным затратам на другой стадии работы.

Для Polar.DB такой вопрос возникает при организации первичного индекса. Часть состояния индекса поддерживается в оперативной памяти, что позволяет уменьшить число обращений к файловому представлению при поиске. Одновременно это состояние требуется подготовить после открытия хранилища. Аналогично, append-ориентированное изменение последовательности уменьшает стоимость собственно изменения, но полная прикладная оценка требует отдельно учитывать фиксацию данных на накопителе.

Таким образом, для оценки текущей реализации требуется не единая величина «производительности», а экспериментальное разделение различных классов операций.

### 1.2. Цель исследования

Цель исследования заключается в экспериментальной оценке основных операций текущей реализации Polar.DB и выявлении тех свойств организации последовательности и индексных построений, которые определяют эффективность работы на массивах различного размера.

### 1.3. Задача исследования

Для достижения цели решаются следующие задачи:

1. Выделить набор операций, характеризующих различные стадии работы хранилища: построение первичного индекса, поиск по первичному и внешнему ключу, добавление, логическое удаление и повторное открытие.
2. Сформировать воспроизводимые модельные данные двух размеров и одинаковые логические задания для Polar.DB и SQLite.
3. Разделить время собственно поиска, время изменения до фиксации и время изменения с фиксацией данных на накопителе.
4. Проверять семантическую правильность результата независимо от измерения времени.
5. Сопоставить полученные характеристики и определить границы, в которых наблюдаемые преимущества и недостатки могут быть отнесены к архитектуре Polar.DB.

### 1.4. Гипотеза исследования

Гипотеза исследования состоит в следующем. Организация данных в виде последовательности и специализированных индексных построений позволяет получить малую стоимость точечного поиска по первичному ключу и append-ориентированных изменений. Поддержание статической части первичного индекса в оперативной памяти дополнительно уменьшает стоимость поиска, однако увеличивает объём работы, необходимой для подготовки индекса после открытия. Для внешнего неуникального индекса при большом числе найденных элементов определяющей становится уже не стоимость нахождения ключа, а стоимость обхода и формирования результата.

### 1.5. Научная новизна и практическая значимость

Научная новизна работы связывается не с введением новой модели базы данных — основные модели PolarDB были опубликованы ранее [1–3], — а с экспериментальной декомпозицией характеристик современной реализации по классам операций. В одной серии измерений отдельно исследуются построение первичного индекса, точечный поиск, выборка по неуникальному индексу с различной мощностью результата, изменение до и после границы фиксации, а также подготовка индексного состояния после открытия.

Практическая значимость заключается в получении характеристик, позволяющих выбирать подходящий режим использования Polar.DB и определять направления дальнейшей оптимизации. Одновременно созданный набор модельных задач и первичных результатов может использоваться для последующего сравнения версий библиотеки.

## 2. Связь с предыдущими работами

В работе [1] PolarDB рассматривалась как система создания специализированных NoSQL баз данных и СУБД. Существенными элементами подхода являлись гибкие средства типовых определений, позволяющие задавать различные схемы структуризации, и формы поддержания структурных значений с отображением в файловые представления.

В работе [2] эти идеи были представлены в виде библиотеки классов для платформы .NET Core. Среди основных задач были выделены структурирование данных, сериализация, отображение на байтовые потоки, индексные построения, работа с динамическими байтовыми потоками, распределение данных, резервное копирование и восстановление. Тем самым индекс не является изолированным компонентом: он рассматривается в связи с типизированным представлением и файловой организацией данных.

В работе [3] в качестве основной абстракции структурированного построения базы данных рассматривается последовательность объектов. Были описаны сериализация и десериализация элементов, универсальная последовательность, универсальный индекс и роль первичного ключа при редактировании данных. Для настоящего исследования особенно существенно именно это сочетание последовательности и индексного построения.

Экспериментальная проверка специализированных вариантов Polar выполнялась и ранее. Например, в [4] исследовалась реализация SPARQL 1.1 и RDF Triple Store. Настоящая работа отличается тем, что сравнение проводится для базовых операций библиотеки и специально разделяет стадии, которые в прикладной задаче часто оказываются смешанными: загрузку, построение индекса, подготовку к поиску, собственно поиск и фиксацию изменений.

## 3. Организация данных в исследуемой версии Polar.DB

### 3.1. Последовательность структурированных элементов

Базовым объектом хранения является последовательность элементов заданного типа. Тип задаёт структуру сериализуемого значения, а последовательность отображается в байтовый поток. Такой подход позволяет отделять логическое представление элемента от конкретного файлового расположения и строить над последовательностью различные формы индексирования.

Исследуемая реализация ориентирована преимущественно на последовательное добавление данных. Логическое изменение записи может быть представлено добавлением нового физического состояния, а не произвольной перестройкой ранее записанной области. Это свойство существенно для интерпретации результатов операций добавления и удаления.

### 3.2. Первичный ключ и первичное индексное построение

Первичный ключ однозначно определяет логический элемент. Для ускорения поиска строится первичное индексное представление. В текущей версии статическая часть такого индекса содержит упорядоченное ключевое/хеш-представление и соответствующие смещения элементов в последовательности.

Часть статического состояния после подготовки хранилища размещается в оперативной памяти. В частности, в исследуемой версии вместе с массивом ключевых/хеш-значений сохраняется массив смещений. Это позволяет при точечном поиске избежать дополнительного чтения смещения из индексного файлового потока.

Изменения, выполненные после построения статического индекса, образуют динамическое состояние. Оно имеет приоритет над статическим представлением, что обеспечивает семантику последнего изменения для замены и логического удаления записи.

### 3.3. Внешние индексные построения

Внешний индекс предназначен для поиска по неуникальному признаку. В отличие от первичного ключа, одному значению внешнего ключа может соответствовать множество элементов последовательности. Поэтому время такой операции состоит из нескольких частей: нахождения требуемого диапазона индекса, получения смещений, проверки актуальности записей и формирования результирующего множества.

При малом результате стоимость поиска границы может быть существенной долей общего времени. При тысячах и миллионах найденных записей основная стоимость смещается к обходу и выдаче результата. По этой причине в эксперимент включены два варианта внешнего ключа: обычный, имеющий примерно равномерное распределение, и высокочастотный ключ, соответствующий 40% исходных записей.

### 3.4. Построение индекса и подготовка после открытия

Построение индекса и использование уже построенного индекса являются различными операциями. Аналогично различаются открытие файлового представления и подготовка хранилища к индексному поиску. В Polar.DB после открытия требуется восстановить или загрузить необходимое состояние индекса. Поэтому далее эти стадии измеряются раздельно.

Такое разделение представляется принципиальным: малая величина времени одного поиска не означает автоматически малой стоимости короткоживущего сеанса, если перед первым запросом требуется заметная подготовка.

## 4. Постановка экспериментального исследования

### 4.1. Сравниваемые реализации

В качестве исследуемой системы используется текущая на момент серии версия Polar.DB. В качестве внешней контрольной точки используется SQLite через библиотеку `Microsoft.Data.Sqlite`. Цель сравнения — не доказательство общего превосходства одной системы управления данными над другой, а получение ориентиров для одинаково определённых логических операций.

В публикационной серии зафиксированы:

- Polar.DB assembly `2.1.3.0`;
- `Microsoft.Data.Sqlite` assembly `9.0.4.0`.

Версия нативного ядра SQLite отдельным полем в использованном протоколе не фиксировалась. Это ограничение отмечается в разделе 8 и должно быть устранено в следующей серии.

### 4.2. Модельные данные

Использованы массивы из 50 000 и 5 000 000 записей. Каждая запись содержит первичные ключевые поля типов `int`, `long`, `Guid`, `string`, четыре соответствующих поля внешнего ключа и строковую полезную нагрузку.

Данные формируются детерминированно. Для обычного внешнего ключа используется 1000 различных значений. Поэтому один успешный запрос возвращает приблизительно:

- 50 записей для массива 50 тыс.;
- 5000 записей для массива 5 млн.

Для высокочастотного внешнего ключа одному значению соответствует 40% записей:

- 20 000 записей для массива 50 тыс.;
- 2 000 000 записей для массива 5 млн.

Такой вариант намеренно является тяжёлой модельной задачей и не рассматривается как типичная прикладная частота ключа.

### 4.3. Набор экспериментов

Публикационная серия включает 16 отдельных программ:

1. построение целочисленного первичного индекса над `id`;
2–5. поиск по первичному ключу типов `int`, `long`, `Guid`, `string`;
6–9. поиск по обычному внешнему ключу тех же четырёх типов;
10–13. поиск по высокочастотному внешнему ключу четырёх типов;
14. добавление;
15. логическое удаление;
16. повторное открытие.

Такое разбиение не позволяет скрыть стоимость одной стадии внутри другой и, в частности, не включает загрузку и построение индекса во время измерения уже подготовленного поиска.

### 4.4. Изоляция систем

Каждый обычный запуск экспериментальной программы сначала работает как координатор. Координатор запускает SQLite и Polar.DB в отдельных дочерних процессах, ожидает их завершения, проверяет сведения о версии исходного кода и конфигурации сборки, после чего формирует объединённый результат.

Для всей полной серии назначается один идентификатор запуска. Если порядок явно не задан, последовательность запуска SQLite и Polar.DB изменяется детерминированно от эксперимента к эксперименту. Это уменьшает систематическое влияние постоянной позиции «первой» или «второй» системы.

### 4.5. Правильность результата

Измерение времени принимается только вместе с проверкой результата. Для операций жизненного цикла заранее вычисляются ожидаемое число строк и контрольная сумма. Для поиска независимо проверяются наборы ключей, используемые в пакетных измерениях и в измерениях единичной задержки.

При независимой проверке архива серии `20260810T030729570Z` совпали все 112 проверяемых соответствий ожидаемых и фактических значений. Таким образом, в таблицы ниже не включались результаты, полученные за счёт пропуска части требуемых данных.

## 5. Вычислительная среда

Эксперименты выполнены на одном компьютере под управлением Windows. Контрольный снимок среды был сделан после серии на том же `main` commit и при чистом рабочем дереве.

Основные параметры:

- Microsoft Windows 11 Pro, version/build `10.0.26200`, x64;
- Intel Core i5-12400;
- 6 физических ядер, 12 логических процессоров;
- 31,78 GiB физической памяти, доступной Windows;
- 4 модуля по 8 GiB, настроенная скорость 3200 MT/s;
- Samsung SSD 980 PRO 250GB;
- SSD, интерфейс NVMe;
- файловая система рабочего тома `D:` — NTFS;
- объём тома 232,16 GiB;
- свободное место при контрольном снимке — 19,56 GiB;
- активный план электропитания Windows — «Сбалансированная».

Модули памяти представлены двумя парами: Patriot Memory `4400 C19 Series` и Gloway `TAC4U3200E16081C`. WMI возвращает различающиеся значения поля `Speed` (2133 и 2400), но одинаковое `ConfiguredClockSpeed = 3200`; поэтому в основном тексте используется только формулировка «настроенная скорость 3200 MT/s».

Публикационный запуск выполнен в конфигурации `Release`; benchmark manifest фиксирует `.NET 10.0.7`, x64, Server GC = `false`. Явные переопределения Tiered Compilation, Tiered PGO и ReadyToRun отсутствовали, использовались значения среды выполнения по умолчанию.

При контрольном снимке активным был .NET SDK `10.0.203`, установлены SDK `8.0.420`, `10.0.100`, `10.0.203`. Следует отметить, что `global.json` на исследуемом commit содержит `sdk/version = 10.0.0`, что не является корректным номером SDK feature band. Поэтому этот файл не обеспечивал строгой фиксации SDK. Конкретный runtime серии фиксируется manifest, а номер SDK 10.0.203 относится к контрольному снимку после серии и не используется как доказанное поле каждого уже выполненного процесса.

## 6. Экспериментальное исследование

Во всех таблицах время приводится в миллисекундах. Основной показатель — медиана повторных измерений. Величины поиска в подготовленном состоянии относятся к среднему времени одного запроса внутри измеряемого пакета. Размер файлов указан как сумма файлов конкретного экспериментального варианта; MB здесь означает 10^6 байт.

### 6.1. Построение целочисленного первичного индекса

Эксперимент выделяет построение индекса по `id` и последующий `flush`. Загрузка исходных значений в измеряемую величину не входит. Выполняются три разогревающих и десять измеряемых построений.

| Число записей | Polar.DB, мс | SQLite, мс | Отношение SQLite / Polar.DB |
|---:|---:|---:|---:|
| 50 000 | **9,800** | 16,614 | 1,70 |
| 5 000 000 | **186,797** | 1488,885 | 7,97 |

На большом массиве различие существенно увеличилось. Для 5 млн записей размер файлового представления данного опыта составил примерно 100,0 MB у Polar.DB и 118,6 MB у SQLite.

Полученный коэффициент относится только к изолированной операции построения данного первичного индекса. Он не характеризует полную загрузку произвольной базы данных и не должен переноситься на построение других типов индексов без отдельного эксперимента.

### 6.2. Поиск по первичному ключу

Для каждого типа ключа рассматриваются две фазы: поиск после повторного открытия без специального разогрева файлов и поиск в подготовленном состоянии после разогрева. Термин «без разогрева» относится к протоколу программы и не означает принудительного очищения системного файлового кэша Windows.

Ниже приведена медиана поиска в подготовленном состоянии.

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн | Отношение на 5 млн |
|---|---:|---:|---:|---:|---:|
| `int` | **0,00560** | 0,00601 | **0,00406** | 0,00794 | 1,95 |
| `long` | **0,00544** | 0,00724 | **0,00392** | 0,00847 | 2,16 |
| `Guid` | **0,00556** | 0,00691 | **0,00381** | 0,01416 | 3,72 |
| `string` | **0,00626** | 0,01175 | **0,00458** | 0,00755 | 1,65 |

Для всех четырёх типов Polar.DB показал меньшее время поиска. На массиве 50 тыс. различие для `int` невелико, тогда как на 5 млн выигрыш наблюдается более отчётливо. Особенно заметен результат для `Guid`.

Размер файлового представления одного из первичных опытов (`int`) составил:

| Число записей | Polar.DB | SQLite |
|---:|---:|---:|
| 50 000 | 5,75 MB | 14,57 MB |
| 5 000 000 | 575,00 MB | 1688,56 MB |

На 5 млн записей файловое представление Polar.DB в данном опыте примерно в 2,94 раза меньше. Эта величина не является оценкой потребления оперативной памяти: часть первичного индекса Polar.DB в подготовленном состоянии размещается в памяти процесса.

### 6.3. Поиск по обычному внешнему ключу

Для обычного внешнего ключа одному значению соответствует приблизительно 1/1000 массива. Поэтому при 50 тыс. записей запрос выдаёт около 50 элементов, при 5 млн — около 5000.

Результаты поиска в подготовленном состоянии:

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| `int` | 0,195 | **0,090** | 24,562 | **22,083** |
| `long` | 0,198 | **0,093** | 23,865 | **22,176** |
| `Guid` | 0,202 | **0,096** | 24,202 | **24,144** |
| `string` | 0,205 | **0,105** | 27,085 | **22,392** |

На 50 тыс. записей SQLite приблизительно вдвое быстрее Polar.DB для данного типа выборки. При 5 млн различие уменьшается: для `Guid` результаты практически совпадают, для остальных типов SQLite быстрее примерно на 8–21%.

Этот результат важен для интерпретации первичного поиска. Выигрыш при нахождении единственной записи по первичному ключу не переносится автоматически на неуникальное индексное построение. При росте числа найденных записей существенную роль играют получение смещений, проверка актуальности и формирование результата.

Для обычного внешнего индекса на 5 млн записей размер файлов составил около 635,0 MB у Polar.DB и 1688,6 MB у SQLite в использованном варианте опыта.

### 6.4. Поиск по высокочастотному внешнему ключу

В следующем модельном варианте одному внешнему ключу соответствует 40% исходных элементов. Это даёт 20 тыс. элементов результата на малом массиве и 2 млн элементов на большом.

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| `int` | 75,761 | **35,614** | 5408,341 | **3654,167** |
| `long` | 84,379 | **37,263** | 5562,963 | **3580,304** |
| `Guid` | 88,425 | **36,288** | 5323,905 | **3720,585** |
| `string` | 76,033 | **37,439** | 5499,012 | **3643,057** |

При 5 млн записей SQLite быстрее примерно в 1,43–1,55 раза. В таком режиме само нахождение диапазона индекса составляет только часть общей работы: требуется получить и обработать два миллиона записей. Этот опыт показывает границу применимости оптимизаций точечного поиска и отдельно характеризует массовую выдачу по внешнему ключу.

### 6.5. Добавление и логическое удаление

Для изменения данных используются две различные характеристики.

Первая — время изменения **до границы фиксации**. Для SQLite измеряемые операции выполняются внутри открытой транзакции; её заключительная фиксация не входит в отдельные значения. Для Polar.DB аналогично измеряется изменение рабочего состояния до общего `Flush`.

Вторая — время изменения **с фиксацией**. Измеряется среднее время одной операции в пакете из 100 изменений, включающем фиксацию и синхронизацию файлов с накопителем. Выполняются два разогревающих и 15 измеряемых пакетов.

Результаты:

| Операция | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| добавление до фиксации | **0,0021** | 0,0319 | **0,0006** | 0,1108 |
| добавление с фиксацией | 0,300 | **0,203** | **0,310** | 0,678 |
| удаление до фиксации | **0,0018** | 0,0213 | **0,0005** | 0,0408 |
| удаление с фиксацией | 0,289 | **0,192** | **0,276** | 0,354 |

До фиксации Polar.DB заметно быстрее на обоих размерах. Однако результат с фиксацией зависит от размера исходного массива. На 50 тыс. записей SQLite быстрее примерно в 1,5 раза; на 5 млн Polar.DB быстрее примерно в 2,19 раза для добавления и в 1,28 раза для удаления.

Именно различие этих двух таблиц измерений показывает, почему нельзя характеризовать append-операцию только временем изменения структуры в памяти. Для прикладной системы, требующей подтверждённой записи на накопитель, существенна вторая величина.

### 6.6. Повторное открытие и подготовка индексного состояния Polar.DB

Для Polar.DB измерялись две стадии:

1. открытие файловых потоков без требования выполнить индексный запрос;
2. открытие с подготовкой индексного состояния и выполнением одного поиска по первичному ключу.

| Число записей | Только открытие Polar.DB, мс | Открытие с подготовкой к поиску, мс |
|---:|---:|---:|
| 50 000 | 1,280 | 13,642 |
| 5 000 000 | 1,084 | 701,691 |

Само открытие файлов меняется слабо, тогда как подготовка к первичному поиску заметно растёт с объёмом данных. На 5 млн записей именно подготовка индексных массивов составляет основную часть этой операции.

Первоначальный benchmark также сформировал значения для SQLite — 0,00065 мс для открытия и 0,0133 мс для варианта с запросом на 5 млн записей. **Эти значения в межсистемное сравнение настоящей статьи не включаются.** Строгая проверка программы показала, что соединение создавалось строкой вида `Data Source=...` без `Pooling=False`. Для `Microsoft.Data.Sqlite` пул соединений включён по умолчанию. Следовательно, повторные `Open()` могли получать уже подготовленное соединение из пула и не соответствовали физическому повторному открытию, тогда как Polar.DB выполнял собственную процедуру открытия и подготовки.

Для корректного межсистемного эксперимента требуется отдельная новая серия с явно отключённым пулом или с очисткой пула между измерениями. До такого запуска можно делать вывод о масштабировании подготовки Polar.DB, но нельзя количественно утверждать превосходство SQLite над Polar.DB в операции повторного открытия по данным этой серии.

### 6.7. Изменение после размещения смещений первичного индекса в памяти

До основной серии была выполнена публикационная серия `20260808T095444545Z` на commit `3f8476f34b72b1ec18f875b3ac35143a00adac42`. Между этой точкой и основной серией в исследуемом пути первичного индекса было добавлено хранение статических смещений в оперативной памяти рядом с массивом ключевых/хеш-значений. Остальные изменения в рассматриваемом diff относятся к тестам.

Для 5 млн записей время поиска в подготовленном состоянии изменилось следующим образом:

| Тип ключа | До, мкс/запрос | После, мкс/запрос | Изменение |
|---|---:|---:|---:|
| `int` | 5,284 | **4,065** | −23,1% |
| `long` | 5,488 | **3,921** | −28,6% |
| `Guid` | 5,862 | **3,811** | −35,0% |
| `string` | 10,049 | **4,585** | −54,4% |

Одновременно собственное время Polar.DB для открытия с подготовкой к поиску изменилось приблизительно с 564,5 до 701,7 мс.

Это наблюдение соответствует ожидаемому компромиссу: дополнительное индексное состояние в оперативной памяти уменьшает стоимость последующего поиска, но требует дополнительной подготовки. Однако две серии выполнялись в разное время, поэтому данное сравнение является сравнением версий, а не строгим чередующимся A/B-экспериментом. Проценты не следует интерпретировать как точную причинную стоимость одной программной оптимизации.

## 7. Обсуждение результатов

Полученные данные позволяют выделить несколько устойчивых для данной серии наблюдений.

Во-первых, специализированное первичное индексное построение Polar.DB хорошо соответствует точечному поиску. На 5 млн записей выигрыш относительно выбранной реализации SQLite наблюдался для всех четырёх типов первичного ключа. При этом преимущество не является одинаковым для всех типов и размеров массива.

Во-вторых, результаты внешнего индекса показывают принципиально другую картину. При выдаче множества записей SQLite не уступает, а чаще превосходит текущую реализацию Polar.DB. Чем больше результат, тем в большей степени измерение характеризует обход и формирование записей, а не собственно нахождение ключа.

В-третьих, для операций изменения важно наличие границы фиксации. Очень малое время изменения до фиксации является характеристикой append-ориентированного рабочего пути, но само по себе не характеризует надёжно сохранённую операцию. После включения синхронизации разница существенно уменьшается и даже меняет знак на малом массиве.

В-четвёртых, результаты по двум размерам данных показывают необходимость исследовать масштабирование. Например, построение первичного индекса Polar.DB относительно SQLite различается примерно в 1,7 раза на 50 тыс. и почти в 8 раз на 5 млн. Для фиксируемых изменений, наоборот, преимущество Polar.DB появляется только на большом исходном массиве.

В-пятых, файловое представление Polar.DB в исследованных вариантах существенно меньше. Для первичного поиска на 5 млн это около 575 MB против 1689 MB. Однако этот результат необходимо рассматривать вместе с использованием оперативной памяти для индексных массивов. Малый размер файлов не означает автоматически меньшего полного потребления памяти системой.

Наконец, повторное открытие показывает внутреннюю стоимость подготовки Polar.DB: при переходе от 50 тыс. к 5 млн записей время варианта «открыть и быть готовым к первичному поиску» возрастает примерно с 13,6 до 701,7 мс, тогда как собственно открытие файлов остаётся около 1 мс. Межсистемное сравнение этой операции отложено до исправления методики SQLite.

Таким образом, эксперимент не даёт основания для формулировки «Polar.DB быстрее SQLite» как общего утверждения. Более точным является вывод о различном профиле эффективности: Polar.DB выигрывает на исследованном первичном индексном построении, точечном поиске по первичному ключу и ряде операций с большим состоянием, но уступает в массовой выборке по неуникальному индексу; часть выигрыша точечного поиска достигается за счёт подготовки индексного состояния в оперативной памяти.

## 8. Ограничения исследования

При интерпретации результатов необходимо учитывать следующие ограничения.

1. Эксперименты выполнены на одной Windows-машине с одним процессором и одним NVMe-накопителем. Результаты нельзя без проверки переносить на Linux, другое оборудование или иной режим нагрузки.
2. Сравниваются конкретные программные реализации одинаковых логических операций. Это не исчерпывающий обзор всех вариантов настройки SQLite и Polar.DB.
3. Фаза «без разогрева» не очищает системный файловый кэш. Поэтому это отсутствие специального разогрева в программе, а не гарантированно холодный физический накопитель.
4. Измерение изменения до фиксации не является измерением надёжной записи. Для прикладного вывода оно должно рассматриваться вместе с пакетным измерением, включающим синхронизацию.
5. Суммарный размер файлов экспериментального каталога не равен полному потреблению ресурсов. В частности, он не включает индексные массивы, размещённые в оперативной памяти.
6. Снимки managed heap, private bytes и working set сохраняются в исходных результатах, но текущий протокол не измеряет гарантированный пик памяти. Поэтому в статье не приводится утверждение о сравнении пикового потребления ОЗУ.
7. Версия `Microsoft.Data.Sqlite` assembly зафиксирована, но версия нативного SQLite отдельно не записана. В следующей серии следует сохранять результат `select sqlite_version()`.
8. `global.json` на исследуемом commit содержит некорректное значение SDK `10.0.0`; поэтому точная фиксация SDK публикационной серии отсутствует. Runtime `.NET 10.0.7` зафиксирован самим benchmark manifest.
9. Значения SQLite из `reopen-only` не пригодны для межсистемного вывода из-за включённого по умолчанию пула соединений. Этот дефект методики обнаружен при рецензировании и явно исключён из выводов статьи.
10. Сравнение двух версий Polar.DB до и после хранения смещений первичного индекса в памяти выполнено в разных полных сериях, а не в одном чередующемся A/B-опыте.

## 9. Заключение

В работе проведено экспериментальное исследование основных операций библиотеки Polar.DB на массивах из 50 тыс. и 5 млн структурированных записей. Были раздельно рассмотрены построение первичного индекса, поиск по первичному и внешнему ключу, добавление, логическое удаление и подготовка хранилища после открытия.

Полученные результаты показывают следующее.

1. В исследуемом варианте построение целочисленного первичного индекса Polar.DB выполняется быстрее выбранной реализации SQLite; на 5 млн записей отношение медиан составляет около 7,97.
2. Поиск одной записи по первичному ключу в подготовленном состоянии также быстрее для всех четырёх исследованных типов. На 5 млн записей отношение составляет примерно от 1,65 до 3,72.
3. Для внешнего неуникального индекса преимущество отсутствует. При результате около 5000 строк SQLite немного быстрее либо практически равен Polar.DB, а при результате 2 млн строк быстрее примерно в 1,43–1,55 раза.
4. Append-ориентированная организация даёт малое время добавления и логического удаления до фиксации. При включении фиксации результат зависит от размера исходного массива: на 50 тыс. быстрее SQLite, на 5 млн — Polar.DB.
5. Файловое представление Polar.DB в исследованных сценариях существенно меньше, однако часть индексного состояния переносится в оперативную память.
6. Подготовка первичного индекса Polar.DB после открытия является отдельной заметной операцией и масштабируется с размером данных. Корректное численное сравнение этой стадии с SQLite требует повторного эксперимента с отключённым пулом соединений.
7. Размещение статических смещений первичного индекса в оперативной памяти в новой версии сопровождается уменьшением времени точечного поиска на 23–54% относительно предыдущей серии, но это наблюдение требует отдельного A/B-эксперимента для строгой причинной оценки.

В целом результаты подтверждают целесообразность раздельного анализа операций специализированного хранилища. Для дальнейшего развития Polar.DB представляются наиболее важными два направления: уменьшение стоимости подготовки индексного состояния после открытия и оптимизация формирования больших результатов по неуникальному внешнему индексу. Одновременно необходимо сохранить полученное преимущество первичного поиска и компактность файлового представления.

---

# Приложение А. Подробная методика экспериментов

## А.1. Принцип сравнения

Экспериментальный стенд сравнивает одинаковый внешний результат операции, а не одинаковое внутреннее устройство систем. Для каждого опыта задаётся логическая задача, которую независимо выполняют SQLite и Polar.DB.

Не смешиваются следующие стадии:

- загрузка исходных данных;
- построение индекса;
- открытие файлов;
- подготовка индексных структур;
- собственно поиск;
- изменение рабочего состояния;
- фиксация и синхронизация с накопителем.

Это позволяет связывать измеренную величину с конкретной стадией.

## А.2. Структура запуска

Обычная экспериментальная программа запускается как координатор. Координатор:

1. создаёт идентификатор серии;
2. фиксирует сведения о среде;
3. запускает worker SQLite отдельным процессом;
4. запускает worker Polar.DB отдельным процессом;
5. проверяет совпадение commit и параметров Release-сборки;
6. проверяет форму и ожидаемые значения результатов;
7. объединяет исходные измерения;
8. сохраняет JSON, CSV, manifest и производный HTML-отчёт.

Для полной серии используется один `POLAR_BENCH_RUN_ID`. Порядок двух систем по умолчанию детерминированно чередуется.

## А.3. Параметры поиска по первичному ключу

Для `int`, `long`, `Guid`, `string` используются одинаковые параметры.

Фаза без специального разогрева:

- файловый разогрев: нет;
- разогревающие запросы: 0;
- измеряемые пакеты: 30;
- запросов в пакете: 100;
- пакетных запросов: 3000;
- отдельных измерений задержки: 2000;
- всего измеряемых запросов: 5000.

Фаза в подготовленном состоянии:

- выполняется файловый разогрев;
- разогревающих пакетов: 5;
- запросов в разогревающем пакете: 100;
- всего разогревающих запросов: 500;
- измеряемых пакетов: 100;
- запросов в пакете: 100;
- пакетных запросов: 10 000;
- отдельных измерений задержки: 2000;
- всего измеряемых запросов: 12 000.

Ключи для разогрева, пакетных измерений и отдельных задержек формируются детерминированно, но из разных последовательностей. Это уменьшает зависимость результата от локального участка индекса.

## А.4. Параметры обычного внешнего индекса

Цель одного измеряемого пакета — получить около 20 000 строк результата.

При 50 тыс. исходных записей:

- около 50 строк на запрос;
- 400 запросов на пакет.

При 5 млн:

- около 5000 строк на запрос;
- 4 запроса на пакет.

Число пакетов:

- без специального разогрева — 15;
- в подготовленном состоянии — 30;
- разогревающих пакетов — 3;
- отдельных запросов задержки — 100.

Поэтому сравнение отражает не только нахождение ключа, но и выдачу примерно одинакового суммарного числа строк на пакет.

## А.5. Параметры высокочастотного внешнего индекса

Высокочастотному ключу соответствует 40% массива. Используется один запрос на пакет.

- разогревающих пакетов: 2;
- измеряемых пакетов без разогрева: 5;
- измеряемых пакетов в подготовленном состоянии: 5;
- отдельных измерений задержки: 5.

Для 5 млн записей каждый запрос выдаёт 2 млн элементов, поэтому количество повторов намеренно невелико.

## А.6. Построение первичного индекса

`build-primary-int-only-id-only-experiment` использует упрощённое хранилище только с идентификатором и выделяет стоимость построения целочисленного первичного индекса.

- разогревающих операций: 3;
- измеряемых операций: 10;
- основная метрика: `build + flush`;
- загрузка исходных значений измеряется отдельно и не входит в основную таблицу статьи.

## А.7. Добавление и удаление

Для измерения изменения до фиксации:

- разогревающих операций: 200;
- измеряемых операций: 2000.

Для измерения с фиксацией:

- разогревающих пакетов: 2;
- измеряемых пакетов: 15;
- операций в пакете: 100.

Для SQLite пакет содержит transaction commit, `Flush`, WAL checkpoint в реализации `SqliteStore.Flush` и синхронизацию файлов каталога. Для Polar.DB пакет содержит `Flush` и синхронизацию файлов. В таблице приводится время пакета, делённое на 100 операций.

## А.8. Повторное открытие

Для Polar.DB и SQLite исходная программа задавала:

- разогревающих повторов: 5;
- измеряемых повторов: 30.

Формально были выделены:

- `open-only` — открыть и закрыть хранилище;
- `query-ready reopen` — открыть, подготовить состояние и выполнить один поиск по первичному ключу.

После рецензирования выявлено, что SQLite использовал стандартную строку `Data Source=<file>` без отключения пула. Поскольку pooling в `Microsoft.Data.Sqlite` включён по умолчанию, SQLite-часть этого опыта не обеспечивает требуемой семантики независимого повторного открытия. Она сохраняется в исходных данных, но исключается из научного сравнения.

Исправленный вариант должен использовать, например, `Pooling=False` либо гарантированную очистку пула между повторениями. После такой модификации требуется новая полная или по крайней мере новая изолированная серия с отдельным run id.

## А.9. Проверка корректности

Для lifecycle-операций заранее вычисляются число строк и контрольная сумма ожидаемого состояния.

Для каждой фазы поиска независимо вычисляются:

- ожидаемое число строк для пакетного набора ключей;
- его контрольная сумма;
- ожидаемое число строк для набора единичных запросов;
- его контрольная сумма.

Worker возвращает фактические значения, а координатор проверяет согласованность планов двух систем. Дополнительная проверка публикационного архива основной серии показала 112 совпадений из 112 проверяемых пар expected/factual.

## А.10. Исходные и производные результаты

Для каждого эксперимента сохраняются последние производные файлы:

- `<experiment>.html`;
- `<experiment>.manifest.json`;
- `<experiment>.raw.json`;
- `<experiment>.raw.csv`.

Неизменяемые материалы конкретной серии находятся по пути:

```text
benchmarks/results/raw/<experiment>/<run-id>/
```

Для основной серии:

```text
benchmarks/results/raw/<experiment>/20260810T030729570Z/
```

В каталоге сохраняются объединённый raw JSON, manifest, CSV с измерениями и отдельные результаты двух worker-процессов.

Для количественного анализа первичными считаются raw JSON/CSV; HTML является производным представлением.

## А.11. Публикационная готовность

Benchmark assembly содержит сведения о конфигурации сборки. Запуск помечается `publicationReady` только при оптимизированной `Release`-сборке. Manifest также фиксирует:

- commit;
- признак чистого рабочего дерева;
- .NET runtime;
- архитектуру ОС и процесса;
- Server GC;
- явные `DOTNET_`/`COMPlus_` overrides для Tiered Compilation, Tiered PGO и ReadyToRun;
- версии загруженных Polar.DB и `Microsoft.Data.Sqlite` assemblies;
- сведения о рабочем диске.

Основная серия имеет `publicationReady = true` и чистое рабочее дерево.

## А.12. Полный запуск серии

Из корня репозитория используется команда:

```powershell
pwsh -File .\benchmarks\scripts\run-new-benchmarks.ps1
```

Перед новым публикационным запуском рекомендуется отдельно выполнить Release build и полный набор тестов.

---

# Приложение Б. Технические сведения о публикационной серии

## Б.1. Идентификация исходного кода

- series id: `20260810T030729570Z`;
- branch при контрольном снимке: `main`;
- commit: `e093da0247ec58c7fb78fc381eca52fa002b0967`;
- working tree: clean;
- конфигурация benchmark assemblies: `Release`;
- `publicationReady = true`.

Аппаратный снимок был получен после серии, но на том же commit и чистом рабочем дереве. Он дополняет автоматически сформированные manifest-файлы.

## Б.2. Операционная система

- Microsoft Windows 11 Pro;
- version/build `10.0.26200`;
- x64;
- план электропитания: «Сбалансированная» (`381b4222-f694-41f0-9685-ff5bb260df2e`).

Runtime manifest может выводить ОС как `Microsoft Windows 10.0.26200`; это номер NT version, а не указание редакции Windows 10. WMI-снимок определяет редакцию как Windows 11 Pro.

## Б.3. Процессор

- `12th Gen Intel(R) Core(TM) i5-12400`;
- 6 физических ядер;
- 12 логических процессоров;
- 12 логических процессоров доступны .NET process;
- WMI `MaxClockSpeed`: 2500 MHz;
- L2 cache по WMI: 7680 KiB;
- L3 cache по WMI: 18432 KiB.

`MaxClockSpeed` не рассматривается как фактическая постоянная частота во время измерений.

## Б.4. Оперативная память

- физическая память, видимая системе: 31,78 GiB;
- 4 × 8 GiB;
- configured clock speed: 3200 MT/s.

Состав модулей:

| Количество | Производитель | Part number | WMI Speed | ConfiguredClockSpeed |
|---:|---|---|---:|---:|
| 2 | Patriot Memory (PDP Systems) | `4400 C19 Series` | 2133 | 3200 |
| 2 | Gloway International Co Ltd | `TAC4U3200E16081C` | 2400 | 3200 |

Конфигурация смешанная; поэтому значения WMI сохраняются для воспроизводимости, но в тексте не делается вывод о равенстве исходных SPD/JEDEC профилей модулей.

## Б.5. Накопитель

Физический накопитель:

- Samsung SSD 980 PRO 250GB;
- MediaType: SSD;
- BusType: NVMe;
- GPT;
- физический размер по Windows: 232,89 GiB.

Рабочий том `D:`:

- NTFS;
- 232,16 GiB;
- 19,56 GiB свободно при контрольном снимке.

Свободное место после серии не принимается за точное значение для каждого worker-процесса; run-specific сведения находятся в manifest.

## Б.6. .NET

Benchmark manifest основной серии:

- host/runtime `.NET 10.0.7`;
- x64;
- Server GC: `false`;
- Tiered Compilation: runtime default;
- Tiered PGO: runtime default;
- ReadyToRun: runtime default;
- Polar.DB assembly `2.1.3.0`;
- `Microsoft.Data.Sqlite` assembly `9.0.4.0`.

Контрольный `dotnet --info` после серии:

- active SDK `10.0.203`;
- SDK commit `c23858a6d8`;
- MSBuild `18.3.3+c23858a6d`;
- host `10.0.7`;
- RID `win-x64`.

Установленные SDK:

- `8.0.420`;
- `10.0.100`;
- `10.0.203`.

Установленные `Microsoft.NETCore.App` runtimes:

- `8.0.26`;
- `9.0.11`;
- `10.0.0`;
- `10.0.7`.

Также установлены соответствующие WindowsDesktop runtimes и AspNetCore 10.0.0/10.0.7.

## Б.7. `global.json`

На benchmark commit содержится:

```json
{
  "sdk": {
    "version": "10.0.0",
    "rollForward": "latestMajor",
    "allowPrerelease": true
  }
}
```

`dotnet --info` отмечает такое значение как некорректное для `sdk/version`: SDK feature band имеет вид, например, `10.0.100`. Поэтому данная серия не имеет строгого SDK pin через `global.json`.

Это не изменяет уже записанные результаты и сведения о runtime, но для следующей публикационной серии `global.json` следует исправить, после чего новый запуск считать самостоятельной серией.

## Б.8. Методическое замечание о пуле SQLite

В использованном `reopen-only` коде создавалось соединение:

```csharp
new SqliteConnection($"Data Source={db}")
```

Параметр `Pooling` не задавался. В `Microsoft.Data.Sqlite` pooling по умолчанию включён. Поэтому повторное создание объекта `SqliteConnection` и вызов `Open()` не гарантировали новое физическое открытие базы.

По этой причине значения SQLite `reopen-only` сохранены как диагностические исходные данные, но из сравнительных выводов исключены. Исправление этого опыта является обязательным условием перед публикацией численного межсистемного сравнения повторного открытия.

---

# Список литературы

1. Марчук А.Г. PolarDB — система создания специализированных NoSQL баз данных и СУБД // Моделирование и анализ информационных систем. 2014. Т. 21, № 6. С. 169–175.
2. Марчук А.Г. Архитектура и основные особенности библиотеки PolarDB работы со структурированными данными // Системная информатика. 2018. № 13. С. 25–34. DOI: 10.31144/si.2307-6410.2018.n13.p25-34.
3. Марчук А.Г. Последовательность как абстракция структурированного построения баз данных // Системная информатика. 2021. № 18. С. 35–52. DOI: 10.31144/si.2307-6410.2021.n18.p35-52.
4. Марчук А.Г., Лештаев С.В. Экспериментальная реализация Sparql-1.1 и RDF Triple Store // Аналитика и управление данными в областях с интенсивным использованием данных: труды XVII Международной конференции DAMDID/RCDL’2015. Обнинск, 13–16 октября 2015 г. С. 83–87.
5. Microsoft. Microsoft.Data.Sqlite: Connection strings. Документация параметров строки соединения, в том числе `Pooling`.

## Служебные материалы воспроизводимости

Исходные данные основной серии находятся в репозитории в каталогах:

```text
benchmarks/results/raw/<experiment>/20260810T030729570Z/
```

Текущий черновик намеренно не содержит утверждения об общем превосходстве Polar.DB над SQLite. Все коэффициенты относятся к явно указанным операциям, размеру данных и состоянию хранилища.