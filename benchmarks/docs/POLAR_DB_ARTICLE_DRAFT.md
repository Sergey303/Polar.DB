# Экспериментальное исследование эффективности последовательностей и индексных построений библиотеки Polar.DB

УДК 004.65

> Единый рабочий черновик статьи. Основные результаты поиска, построения индексов и изменений относятся к серии `20260810T030729570Z`, commit `e093da0247ec58c7fb78fc381eca52fa002b0967`. Исправленный эксперимент повторного открытия относится к серии `20260811T060237246Z-eed96558-reopen-only`, commit `eed965582a1f0b1e6d0796c09528f0b7876b1ba4`.

## Аннотация

В статье продолжается исследование библиотеки Polar.DB, предназначенной для создания специализированных средств структурирования, хранения и обработки данных. Рассматривается современная реализация последовательности объектов и индексных построений, а также экспериментально оцениваются характеристики основных операций: построение первичного индекса, поиск по первичному и внешнему ключу, добавление и логическое удаление записей, повторное открытие хранилища. В качестве внешней точки сравнения используется SQLite. Эксперименты выполнены на массивах из 50 тыс. и 5 млн записей; обе системы получают одинаковые логические задания, а правильность результата контролируется по числу записей и контрольным суммам.

На массиве из 5 млн записей Polar.DB построил исследуемый целочисленный первичный индекс за 186,8 мс против 1488,9 мс у SQLite. В подготовленном состоянии поиск одной записи по первичному ключу для типов `int`, `long`, `Guid` и `string` оказался быстрее в 1,65–3,72 раза. При выборке по неуникальному внешнему ключу, когда один запрос возвращал около 5000 записей, SQLite был быстрее на 0–21%, а при высокочастотном ключе с результатом 2 млн записей — примерно в 1,43–1,55 раза. Для добавления и удаления обнаружена зависимость результата от размера исходного массива и от включения фиксации изменений на накопителе.

Повторная рецензия исходной методики выявила, что первое измерение повторного открытия SQLite выполнялось при включённом по умолчанию пуле соединений. После исправления эксперимента SQLite использует `Pooling=False`, а существующие пулы очищаются до измерения. В исправленной серии медиана операции «открыть хранилище, подготовить его к индексному поиску и выполнить один поиск по первичному ключу» на 5 млн записей составила 0,816 мс для SQLite и 775,7 мс для Polar.DB. На 50 тыс. записей соответствующие значения составили 0,827 и 15,67 мс. Эксперимент не очищает системный файловый кэш, поэтому эти величины характеризуют повторное открытие в условиях данного программного стенда, а не физически холодный старт накопителя.

Полученные результаты показывают, что эффективность специализированного хранилища следует характеризовать не одной интегральной величиной, а набором характеристик различных стадий работы. Для Polar.DB наиболее заметным является сочетание быстрого точечного поиска по первичному ключу, небольшого файлового представления и малой стоимости append-ориентированных изменений с дополнительными затратами на подготовку индексного состояния после открытия и с меньшей эффективностью массовой выдачи по неуникальному индексу.

**Ключевые слова:** Polar.DB, структурированные данные, база данных, последовательность, первичный ключ, индексное построение, внешний индекс, SQLite, экспериментальное исследование.

## 1. Введение

Развитие информационных систем сопровождается увеличением объёмов обрабатываемых структурированных данных и разнообразия схем их организации. Универсальные системы управления базами данных решают широкий класс задач, однако для ряда предметных областей существенное значение имеет возможность построения специализированных средств хранения, в которых представление данных и индексные построения непосредственно соответствуют структуре решаемой задачи.

Система PolarDB была предложена как средство создания специализированных баз данных и СУБД [1]. В дальнейшем была разработана библиотека PolarDB работы со структурированными данными, охватывающая типовые определения, сериализацию, отображение данных на байтовые потоки, индексные построения и другие задачи [2]. Следующим шагом стала модель базы данных, основанная на последовательности объектов, и универсальные средства индексирования последовательности [3]. В современной версии проекта используется написание **Polar.DB**; написание PolarDB в ссылках [1–3] сохранено в соответствии с опубликованными работами.

Ранее экспериментальные реализации Polar применялись, в частности, для хранения RDF и выполнения запросов SPARQL [4]. Настоящая работа продолжает это направление, но предметом эксперимента являются базовые операции универсального хранилища: построение индекса, поиск, изменение последовательности и подготовка индексного состояния после повторного открытия.

### 1.1. Актуальность исследования

Актуальность работы определяется необходимостью оценивать системы хранения не только по функциональности, но и по стоимости основных операций при увеличении объёма данных. Для специализированного хранилища архитектурное решение, выгодное для одной операции, может приводить к дополнительным затратам на другой стадии работы.

Для Polar.DB такой вопрос возникает при организации первичного индекса. Статическая часть индексного состояния поддерживается в оперативной памяти, что позволяет уменьшить число обращений к файловому представлению при поиске. Одновременно это состояние необходимо подготовить после открытия хранилища. Аналогично, append-ориентированное изменение последовательности уменьшает стоимость собственно изменения, но прикладная оценка должна отдельно учитывать фиксацию данных на накопителе.

Следовательно, для оценки текущей реализации требуется не единая величина «производительности», а экспериментальное разделение различных классов операций.

### 1.2. Цель исследования

Цель исследования заключается в экспериментальной оценке основных операций текущей реализации Polar.DB и выявлении свойств организации последовательности и индексных построений, определяющих эффективность работы на массивах различного размера.

### 1.3. Задача исследования

Для достижения цели решаются следующие задачи:

1. выделить операции, характеризующие различные стадии работы хранилища: построение первичного индекса, поиск по первичному и внешнему ключу, добавление, логическое удаление и повторное открытие;
2. сформировать воспроизводимые модельные данные двух размеров и одинаковые логические задания для Polar.DB и SQLite;
3. разделить время поиска, время изменения до фиксации и время изменения с фиксацией данных на накопителе;
4. проверять семантическую правильность результата независимо от измерения времени;
5. сопоставить полученные характеристики и определить границы применимости наблюдаемых преимуществ и недостатков.

### 1.4. Гипотеза исследования

Гипотеза исследования состоит в следующем. Организация данных в виде последовательности и специализированных индексных построений позволяет получить малую стоимость точечного поиска по первичному ключу и append-ориентированных изменений. Поддержание статической части первичного индекса в оперативной памяти дополнительно уменьшает стоимость поиска, однако увеличивает объём работы, необходимой для подготовки индекса после открытия. Для внешнего неуникального индекса при большом числе найденных элементов определяющей становится не только стоимость нахождения ключа, но и стоимость обхода и формирования результата.

### 1.5. Научная новизна и практическая значимость

Основные модели PolarDB были опубликованы ранее [1–3]. Новизна настоящей работы связана с экспериментальной декомпозицией характеристик современной реализации по классам операций. В одной системе модельных задач отдельно исследуются построение первичного индекса, точечный поиск, выборка по неуникальному индексу с различной мощностью результата, изменение до и после границы фиксации, а также подготовка индексного состояния после открытия.

Практическая значимость заключается в получении характеристик, позволяющих выбирать режим использования Polar.DB и определять направления дальнейшей оптимизации. Созданный набор модельных задач одновременно служит средством регрессионного сравнения версий библиотеки.

## 2. Связь с предыдущими работами

В работе [1] PolarDB рассматривалась как система создания специализированных NoSQL баз данных и СУБД. Существенными элементами подхода являлись гибкие средства типовых определений и формы отображения структурных значений в файловые представления.

В работе [2] эти идеи были представлены в виде библиотеки классов для платформы .NET Core. Среди основных задач выделялись структурирование данных, сериализация, отображение на байтовые потоки, индексные построения, работа с динамическими байтовыми потоками, распределение данных, резервное копирование и восстановление. Тем самым индекс рассматривается в связи с типизированным представлением и файловой организацией данных.

В работе [3] в качестве основной абстракции структурированного построения базы данных рассматривается последовательность объектов. Были описаны сериализация и десериализация элементов, универсальная последовательность, универсальный индекс и роль первичного ключа при редактировании данных. Для настоящего исследования существенно именно сочетание последовательности и индексного построения.

Экспериментальная проверка специализированных вариантов Polar выполнялась и ранее. Например, в [4] исследовалась реализация SPARQL 1.1 и RDF Triple Store. Настоящая работа отличается тем, что эксперимент проводится для базовых операций библиотеки и специально разделяет стадии, которые в прикладной задаче часто смешиваются: загрузку, построение индекса, подготовку к поиску, собственно поиск и фиксацию изменений.

## 3. Организация данных в исследуемой версии Polar.DB

### 3.1. Последовательность структурированных элементов

Базовым объектом хранения является последовательность элементов заданного типа. Тип задаёт структуру сериализуемого значения, а последовательность отображается в байтовый поток. Такой подход позволяет отделять логическое представление элемента от конкретного файлового расположения и строить над последовательностью различные формы индексирования.

Исследуемая реализация ориентирована преимущественно на последовательное добавление данных. Логическое изменение записи может быть представлено добавлением нового физического состояния, а не произвольной перестройкой ранее записанной области. Это свойство существенно для интерпретации операций добавления и удаления.

### 3.2. Первичный ключ и первичное индексное построение

Первичный ключ однозначно определяет логический элемент. Для ускорения поиска строится первичное индексное представление. В исследуемой версии статическая часть такого индекса содержит ключевое или хеш-представление и соответствующие смещения элементов в последовательности.

Часть статического состояния после подготовки хранилища размещается в оперативной памяти. В частности, вместе с массивом ключевых или хеш-значений сохраняется массив смещений. Это уменьшает стоимость последующего точечного поиска, но требует подготовки индексных массивов после открытия.

Изменения, выполненные после построения статического индекса, образуют динамическое состояние и имеют приоритет над статическим представлением, обеспечивая семантику последнего изменения для замены и логического удаления записи.

### 3.3. Внешние индексные построения

Внешний индекс предназначен для поиска по неуникальному признаку. Одному значению внешнего ключа может соответствовать множество элементов последовательности. Поэтому время такой операции включает нахождение требуемого диапазона индекса, получение смещений, проверку актуальности записей и формирование результирующего множества.

При малом результате стоимость нахождения диапазона может быть существенной долей общего времени. При тысячах и миллионах найденных записей основная стоимость смещается к обходу и выдаче результата. По этой причине в эксперимент включены два варианта внешнего ключа: обычный, имеющий около 1000 различных значений, и высокочастотный, соответствующий 40% исходных записей.

### 3.4. Построение индекса и подготовка после открытия

Построение индекса и использование уже построенного индекса являются различными операциями. Аналогично различаются открытие файлового представления и подготовка хранилища к индексному поиску. В Polar.DB после открытия необходимо загрузить или восстановить состояние первичного индекса в памяти.

Поэтому далее эти стадии измеряются раздельно. Малая величина времени одного поиска не означает автоматически малой стоимости короткоживущего сеанса, если перед первым запросом требуется заметная подготовка.

## 4. Постановка экспериментального исследования

### 4.1. Сравниваемые реализации

В качестве исследуемой системы используется Polar.DB. В качестве внешней контрольной точки используется SQLite через библиотеку `Microsoft.Data.Sqlite`. Цель сравнения состоит не в доказательстве общего превосходства одной системы над другой, а в получении ориентиров для одинаково определённых логических операций.

Для основной серии зафиксированы:

- Polar.DB assembly `2.1.3.0`;
- `Microsoft.Data.Sqlite` assembly `9.0.4.0`;
- .NET runtime `10.0.7`.

В исходной основной серии нативная версия SQLite отдельным полем не сохранялась. Этот недостаток стенда устранён перед исправленным опытом повторного открытия: его manifest фиксирует native SQLite `3.53.3` при той же версии `Microsoft.Data.Sqlite` assembly `9.0.4.0`.

### 4.2. Модельные данные

Использованы массивы из 50 000 и 5 000 000 записей. Каждая запись содержит первичные ключевые поля типов `int`, `long`, `Guid`, `string`, четыре соответствующих поля внешнего ключа и строковую полезную нагрузку.

Данные формируются детерминированно. Для обычного внешнего ключа используется 1000 различных значений. Поэтому один успешный запрос возвращает приблизительно 50 записей для массива 50 тыс. и 5000 записей для массива 5 млн.

Для высокочастотного внешнего ключа одному значению соответствует 40% записей: 20 000 на малом массиве и 2 000 000 на большом. Такой вариант намеренно является тяжёлой модельной задачей и не рассматривается как типичная прикладная частота ключа.

### 4.3. Набор экспериментов

Полный набор включает 16 отдельных сценариев:

1. построение целочисленного первичного индекса над `id`;
2–5. поиск по первичному ключу типов `int`, `long`, `Guid`, `string`;
6–9. поиск по обычному внешнему ключу тех же четырёх типов;
10–13. поиск по высокочастотному внешнему ключу четырёх типов;
14. добавление;
15. логическое удаление;
16. повторное открытие.

Такое разбиение не позволяет скрыть стоимость одной стадии внутри другой и, в частности, не включает загрузку и построение индекса во время измерения уже подготовленного поиска.

### 4.4. Изоляция систем

Координатор запускает SQLite и Polar.DB в отдельных дочерних процессах, проверяет сведения о версии исходного кода и конфигурации сборки, после чего формирует объединённый результат. Для полной серии используется единый идентификатор запуска, а порядок двух систем детерминированно чередуется, если он не задан явно.

Исправленный опыт повторного открытия выполнен отдельной серией, поскольку для него изменена методика SQLite и исправлена фиксация SDK. В этой серии порядок был Polar.DB, затем SQLite.

### 4.5. Правильность результата

Измерение времени принимается только вместе с проверкой результата. Для операций жизненного цикла заранее вычисляются ожидаемое число строк и контрольная сумма. Для поиска независимо проверяются наборы ключей, используемые в пакетных измерениях и в измерениях единичной задержки.

При независимой проверке основной серии совпали 112 из 112 проверяемых соответствий ожидаемых и фактических значений. В исправленном опыте повторного открытия обе системы также возвратили ожидаемое число записей и контрольную сумму для 50 тыс. и 5 млн записей.

## 5. Вычислительная среда

Эксперименты выполнены на одном компьютере под управлением Windows 11 Pro, build 26200, x64.

Основные параметры:

- Intel Core i5-12400;
- 6 физических ядер, 12 логических процессоров;
- 31,78 GiB физической памяти, видимой Windows;
- 4 модуля по 8 GiB, настроенная скорость 3200 MT/s;
- Samsung SSD 980 PRO 250GB, NVMe;
- рабочий том `D:` — NTFS;
- активный план электропитания Windows — «Сбалансированная».

Модули памяти представлены двумя парами: Patriot Memory `4400 C19 Series` и Gloway `TAC4U3200E16081C`. WMI возвращает различающиеся значения поля `Speed`, но одинаковое `ConfiguredClockSpeed = 3200`; поэтому используется только формулировка «настроенная скорость 3200 MT/s».

Основная серия выполнена в `Release`, benchmark manifest фиксирует `.NET 10.0.7`, x64, Server GC = `false`; явные переопределения Tiered Compilation, Tiered PGO и ReadyToRun отсутствовали.

На commit основной серии `global.json` содержал некорректное значение SDK `10.0.0`, поэтому строгая фиксация SDK отсутствовала. Перед исправленным опытом повторного открытия `global.json` изменён на допустимый SDK feature band `10.0.203` с `rollForward = latestPatch`. Локальный запуск подтвердил `dotnet --version = 10.0.203`. Runtime исправленного опыта остался `.NET 10.0.7`.

## 6. Экспериментальное исследование

Во всех таблицах время приводится в миллисекундах. Основной показатель — медиана повторных измерений. Размер файлов указан как сумма файлов конкретного экспериментального варианта; MB означает 10^6 байт.

### 6.1. Построение целочисленного первичного индекса

Эксперимент выделяет построение индекса по `id` и последующий `flush`. Загрузка исходных значений в основную измеряемую величину не входит. Выполняются три разогревающих и десять измеряемых построений.

| Число записей | Polar.DB, мс | SQLite, мс | SQLite / Polar.DB |
|---:|---:|---:|---:|
| 50 000 | **9,800** | 16,614 | 1,70 |
| 5 000 000 | **186,797** | 1488,885 | 7,97 |

Для 5 млн записей размер файлового представления данного опыта составил примерно 100,0 MB у Polar.DB и 118,6 MB у SQLite. Полученный коэффициент относится только к изолированной операции построения данного первичного индекса и не характеризует полную загрузку произвольной базы данных.

### 6.2. Поиск по первичному ключу

Для каждого типа ключа рассматриваются поиск после повторного открытия без специального разогрева и поиск в подготовленном состоянии. Термин «без разогрева» не означает принудительного очищения системного файлового кэша Windows.

Медиана поиска в подготовленном состоянии:

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн | SQLite / Polar.DB на 5 млн |
|---|---:|---:|---:|---:|---:|
| `int` | **0,00560** | 0,00601 | **0,00406** | 0,00794 | 1,95 |
| `long` | **0,00544** | 0,00724 | **0,00392** | 0,00847 | 2,16 |
| `Guid` | **0,00556** | 0,00691 | **0,00381** | 0,01416 | 3,72 |
| `string` | **0,00626** | 0,01175 | **0,00458** | 0,00755 | 1,65 |

Для всех четырёх типов Polar.DB показал меньшее время поиска. На массиве 50 тыс. различие для `int` невелико, тогда как на 5 млн выигрыш выражен сильнее.

В опыте первичного поиска по `int` файловое представление составило около 5,75 MB против 14,57 MB на 50 тыс. записей и 575,0 MB против 1688,6 MB на 5 млн записей для Polar.DB и SQLite соответственно. Это не является оценкой полного потребления памяти: часть первичного индекса Polar.DB находится в оперативной памяти.

### 6.3. Поиск по обычному внешнему ключу

Одному значению обычного внешнего ключа соответствует приблизительно 1/1000 массива: около 50 элементов результата на 50 тыс. записей и около 5000 на 5 млн.

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| `int` | 0,195 | **0,090** | 24,562 | **22,083** |
| `long` | 0,198 | **0,093** | 23,865 | **22,176** |
| `Guid` | 0,202 | **0,096** | 24,202 | **24,144** |
| `string` | 0,205 | **0,105** | 27,085 | **22,392** |

На 50 тыс. записей SQLite приблизительно вдвое быстрее Polar.DB для данного типа выборки. При 5 млн различие уменьшается: для `Guid` результаты практически совпадают, для остальных типов SQLite быстрее примерно на 8–21%.

Следовательно, выигрыш при нахождении единственной записи по первичному ключу не переносится автоматически на неуникальное индексное построение. При росте числа найденных записей существенную роль играют получение смещений, проверка актуальности и формирование результата.

### 6.4. Поиск по высокочастотному внешнему ключу

В этом варианте одному внешнему ключу соответствует 40% исходных элементов: 20 тыс. на малом массиве и 2 млн на большом.

| Тип ключа | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| `int` | 75,761 | **35,614** | 5408,341 | **3654,167** |
| `long` | 84,379 | **37,263** | 5562,963 | **3580,304** |
| `Guid` | 88,425 | **36,288** | 5323,905 | **3720,585** |
| `string` | 76,033 | **37,439** | 5499,012 | **3643,057** |

При 5 млн записей SQLite быстрее примерно в 1,43–1,55 раза. В таком режиме нахождение диапазона индекса составляет только часть общей работы; требуется получить и обработать два миллиона записей.

### 6.5. Добавление и логическое удаление

Для изменения данных используются две характеристики. Первая — время изменения **до границы фиксации**. Вторая — время изменения **с фиксацией**, то есть среднее время одной операции в пакете из 100 изменений, включающем фиксацию и синхронизацию файлов с накопителем.

| Операция | Polar.DB, 50 тыс. | SQLite, 50 тыс. | Polar.DB, 5 млн | SQLite, 5 млн |
|---|---:|---:|---:|---:|
| добавление до фиксации | **0,0021** | 0,0319 | **0,0006** | 0,1108 |
| добавление с фиксацией | 0,300 | **0,203** | **0,310** | 0,678 |
| удаление до фиксации | **0,0018** | 0,0213 | **0,0005** | 0,0408 |
| удаление с фиксацией | 0,289 | **0,192** | **0,276** | 0,354 |

До фиксации Polar.DB заметно быстрее на обоих размерах. Однако результат с фиксацией зависит от размера исходного массива. На 50 тыс. записей SQLite быстрее; на 5 млн Polar.DB быстрее примерно в 2,19 раза для добавления и в 1,28 раза для удаления.

Поэтому время изменения рабочего состояния нельзя выдавать за время надёжно сохранённой операции. Для прикладной системы существенна величина с включённой границей фиксации.

### 6.6. Повторное открытие и подготовка индексного состояния

Первоначальный вариант опыта `reopen-only` оказался методически некорректен для SQLite: строка соединения не отключала pooling, а `Microsoft.Data.Sqlite` использует пул соединений по умолчанию. Повторный `Open()` мог получать ранее открытое физическое соединение. Эти старые значения полностью исключены из сравнительного результата.

Исправленный эксперимент выполнен серией:

`20260811T060237246Z-eed96558-reopen-only`

на commit:

`eed965582a1f0b1e6d0796c09528f0b7876b1ba4`.

После создания набора SQLite выполняется `SqliteConnection.ClearAllPools()`, а каждое измеряемое соединение создаётся с `Pooling=False`. Manifest фиксирует native SQLite `3.53.3`, `publicationReady = true`, чистое рабочее дерево и Release-сборку. Выполняются 5 разогревающих и 30 измеряемых повторов для каждого размера.

Измеряются две стадии:

1. **только открытие** — открыть и закрыть хранилище;
2. **открытие с подготовкой к поиску** — открыть, подготовить метаданные и индексное состояние и выполнить один индексированный поиск по первичному ключу.

Медианы:

| Число записей | SQLite: только открытие | Polar.DB: только открытие | SQLite: с поиском | Polar.DB: с поиском |
|---:|---:|---:|---:|---:|
| 50 000 | **0,08370** | 1,36820 | **0,82720** | 15,67085 |
| 5 000 000 | **0,12035** | 1,16225 | **0,81565** | 775,71060 |

Для 50 тыс. записей отношение медиан операции «открытие с подготовкой и одним поиском» составляет примерно 18,9 в пользу SQLite. Для 5 млн — примерно 951. Большой коэффициент не следует использовать как самостоятельный рекламный результат: он относится к конкретному определению опыта, одной машине и файловому кэшу Windows, который специально не очищается.

Содержательно важнее характер масштабирования. В SQLite первичный B-tree сохраняется в базе данных, поэтому физическое непуллированное открытие и один точечный запрос в этом опыте остаются около 1 мс при увеличении массива с 50 тыс. до 5 млн. В Polar.DB операция включает подготовку RAM-resident состояния первичного индекса и возрастает приблизительно с 15,7 до 775,7 мс.

Само открытие файлов Polar.DB остаётся около 1–1,4 мс и практически не зависит от размера массива. Следовательно, основная добавочная стоимость связана не с открытием файловых дескрипторов, а с подготовкой индексного состояния.

Этот результат непосредственно дополняет результат раздела 6.2: малое время точечного поиска Polar.DB достигается в уже подготовленном состоянии, подготовка которого при 5 млн записей является отдельной заметной операцией. Для длительно живущего хранилища эта стоимость может амортизироваться множеством запросов; для частого открытия короткоживущих экземпляров она может доминировать.

### 6.7. Изменение после размещения смещений первичного индекса в памяти

Предыдущая publication-ready серия `20260808T095444545Z` была выполнена на commit `3f8476f34b72b1ec18f875b3ac35143a00adac42`. Между ней и основной серией в исследуемом пути первичного индекса было добавлено хранение статических смещений в оперативной памяти рядом с массивом ключевых или хеш-значений.

Для 5 млн записей время поиска в подготовленном состоянии изменилось следующим образом:

| Тип ключа | До, мкс/запрос | После, мкс/запрос | Изменение |
|---|---:|---:|---:|
| `int` | 5,284 | **4,065** | −23,1% |
| `long` | 5,488 | **3,921** | −28,6% |
| `Guid` | 5,862 | **3,811** | −35,0% |
| `string` | 10,049 | **4,585** | −54,4% |

В предыдущей и основной сериях собственное время Polar.DB для открытия с подготовкой к поиску изменилось приблизительно с 564,5 до 701,7 мс; в исправленной отдельной серии получено 775,7 мс. Эти прогоны выполнялись в разное время, поэтому различия между ними являются сравнением версий и запусков, а не строгим чередующимся A/B-экспериментом. Проценты ускорения поиска не следует интерпретировать как точную причинную стоимость одной программной оптимизации без специального A/B-опыта.

## 7. Обсуждение результатов

Полученные данные позволяют выделить несколько наблюдений.

Во-первых, специализированное первичное индексное построение Polar.DB хорошо соответствует точечному поиску. На 5 млн записей выигрыш относительно выбранной реализации SQLite наблюдался для всех четырёх типов первичного ключа.

Во-вторых, результаты внешнего индекса показывают другую картину. При выдаче множества записей SQLite не уступает, а чаще превосходит текущую реализацию Polar.DB. Чем больше результат, тем в большей степени измерение характеризует обход и формирование записей, а не собственно нахождение ключа.

В-третьих, для операций изменения принципиально наличие границы фиксации. Очень малое время изменения до фиксации является характеристикой append-ориентированного рабочего пути, но не характеризует надёжно сохранённую операцию. После включения синхронизации разница уменьшается и на малом массиве меняет знак.

В-четвёртых, результаты по двум размерам данных показывают необходимость исследовать масштабирование. Построение первичного индекса Polar.DB относительно SQLite различается примерно в 1,7 раза на 50 тыс. и почти в 8 раз на 5 млн. Для фиксируемых изменений преимущество Polar.DB появляется только на большом исходном массиве.

В-пятых, файловое представление Polar.DB в исследованных основных сценариях существенно меньше. Однако этот результат необходимо рассматривать вместе с использованием оперативной памяти для индексных массивов. Малый размер файлов не означает автоматически меньшего полного потребления памяти системой.

Наконец, исправленный опыт повторного открытия подтверждает центральный архитектурный компромисс. В подготовленном состоянии Polar.DB быстрее SQLite при точечном поиске по первичному ключу, но подготовка этого состояния после открытия масштабируется с объёмом первичного индекса. На 5 млн записей один непуллированный SQLite reopen с точечным запросом занимает медианно около 0,82 мс, тогда как Polar.DB — около 776 мс. При этом собственно открытие файлов Polar.DB занимает лишь около 1,16 мс.

Таким образом, эксперимент не даёт основания для общей формулировки «Polar.DB быстрее SQLite» или обратной ей. Системы имеют различный профиль эффективности. Polar.DB выигрывает на исследованном первичном индексном построении, точечном поиске в подготовленном состоянии и ряде операций с большим состоянием, но уступает в массовой выборке по неуникальному индексу и существенно дороже подготавливает первичный индекс после открытия.

## 8. Ограничения исследования

При интерпретации результатов необходимо учитывать следующие ограничения.

1. Эксперименты выполнены на одной Windows-машине с одним процессором и одним NVMe-накопителем. Результаты нельзя без проверки переносить на Linux, другое оборудование или иной режим нагрузки.
2. Сравниваются конкретные реализации одинаковых логических операций. Это не исчерпывающий обзор вариантов настройки SQLite и Polar.DB.
3. Фаза «без разогрева» и эксперимент повторного открытия не очищают системный файловый кэш. Поэтому речь идёт об отсутствии специального разогрева в программе, а не о гарантированно холодном физическом накопителе.
4. Измерение изменения до фиксации не является измерением надёжной записи. Оно должно рассматриваться вместе с пакетным измерением, включающим синхронизацию.
5. Суммарный размер файлов экспериментального каталога не равен полному потреблению ресурсов и не включает индексные массивы, размещённые в оперативной памяти.
6. Снимки managed heap, private bytes и working set являются диагностическими и не измеряют гарантированный пик памяти.
7. Нативная версия SQLite не была зафиксирована для основной серии `20260810T030729570Z`; для исправленного reopen-опыта она зафиксирована как `3.53.3`.
8. На commit основной серии `global.json` содержал некорректный SDK pin. Для исправленного reopen-опыта этот дефект устранён, и использован SDK `10.0.203`.
9. Первые SQLite-значения `reopen-only`, полученные при включённом pooling, методически отбракованы и нигде не используются для межсистемных выводов. Сравнительный результат повторного открытия основан только на исправленной серии с `Pooling=False`.
10. Сравнение версий Polar.DB до и после хранения смещений первичного индекса в памяти выполнено в разных сериях, а не в одном чередующемся A/B-опыте.
11. Время порядка единиц микросекунд и долей миллисекунды чувствительно к планированию ОС, таймеру и текущему состоянию файлового кэша. Поэтому наиболее надёжны устойчивые различия между классами операций и масштабирование, а не чрезмерно точная интерпретация отдельных коэффициентов.

## 9. Заключение

В работе проведено экспериментальное исследование основных операций библиотеки Polar.DB на массивах из 50 тыс. и 5 млн структурированных записей. Раздельно рассмотрены построение первичного индекса, поиск по первичному и внешнему ключу, добавление, логическое удаление и подготовка хранилища после открытия.

Полученные результаты показывают следующее.

1. В исследуемом варианте построение целочисленного первичного индекса Polar.DB выполняется быстрее выбранной реализации SQLite; на 5 млн записей отношение медиан составляет около 7,97.
2. Поиск одной записи по первичному ключу в подготовленном состоянии быстрее для всех четырёх исследованных типов. На 5 млн записей отношение составляет примерно от 1,65 до 3,72.
3. Для внешнего неуникального индекса преимущество отсутствует. При результате около 5000 строк SQLite немного быстрее либо практически равен Polar.DB, а при результате 2 млн строк быстрее примерно в 1,43–1,55 раза.
4. Append-ориентированная организация даёт малое время добавления и логического удаления до фиксации. При включении фиксации результат зависит от размера исходного массива: на 50 тыс. быстрее SQLite, на 5 млн — Polar.DB.
5. Файловое представление Polar.DB в исследованных основных сценариях существенно меньше, однако часть индексного состояния переносится в оперативную память.
6. Подготовка первичного индекса Polar.DB после открытия является отдельной заметной операцией. В исправленном непуллированном опыте на 5 млн записей медиана открытия с одним первичным поиском составила около 775,7 мс для Polar.DB и 0,816 мс для SQLite; собственно открытие файлов Polar.DB при этом занимает около 1,16 мс.
7. Размещение статических смещений первичного индекса в оперативной памяти между двумя версиями сопровождалось уменьшением времени точечного поиска на 23–54%, однако строгая причинная оценка требует отдельного A/B-эксперимента.

В целом результаты подтверждают целесообразность раздельного анализа стадий работы специализированного хранилища. Для дальнейшего развития Polar.DB наиболее важными представляются уменьшение стоимости подготовки первичного индексного состояния после открытия и оптимизация формирования больших результатов по неуникальному внешнему индексу. Одновременно следует сохранить преимущества точечного первичного поиска и компактность файлового представления.

---

# Приложение А. Методика экспериментов

## А.1. Принцип сравнения

Стенд сравнивает одинаковый внешний результат операции, а не одинаковое внутреннее устройство систем. Раздельно рассматриваются:

- загрузка исходных данных;
- построение индекса;
- открытие файлов;
- подготовка индексных структур;
- собственно поиск;
- изменение рабочего состояния;
- фиксация и синхронизация с накопителем.

## А.2. Структура запуска

Экспериментальная программа запускается как координатор. Координатор создаёт идентификатор серии, фиксирует сведения о среде, запускает worker SQLite и worker Polar.DB отдельными процессами, проверяет commit и Release-конфигурацию, объединяет результаты и сохраняет JSON, CSV, manifest и производный HTML-отчёт.

## А.3. Поиск по первичному ключу

Для типов `int`, `long`, `Guid`, `string` используются одинаковые параметры.

Фаза без специального разогрева:

- разогревающих запросов: 0;
- измеряемых пакетов: 30;
- запросов в пакете: 100;
- отдельных измерений задержки: 2000.

Фаза в подготовленном состоянии:

- файловый разогрев выполняется;
- разогревающих пакетов: 5 по 100 запросов;
- измеряемых пакетов: 100 по 100 запросов;
- отдельных измерений задержки: 2000.

Значение `batchAvgSamplesMs` является средним временем одного запроса внутри измеряемого пакета.

## А.4. Обычный внешний индекс

Цель одного измеряемого пакета — получить около 20 000 строк результата. При 50 тыс. исходных записей один запрос выдаёт около 50 строк и пакет содержит 400 запросов. При 5 млн один запрос выдаёт около 5000 строк и пакет содержит 4 запроса.

Используются 15 измеряемых пакетов без специального разогрева, 3 разогревающих и 30 измеряемых пакетов в подготовленном состоянии, а также 100 отдельных запросов задержки.

## А.5. Высокочастотный внешний индекс

Высокочастотному ключу соответствует 40% массива. Используется один запрос на пакет, два разогревающих пакета и по пять измеряемых пакетов для двух фаз. Для 5 млн записей один запрос выдаёт 2 млн элементов.

## А.6. Построение первичного индекса

`build-primary-int-only-id-only-experiment` использует упрощённое хранилище только с идентификатором и выделяет стоимость построения целочисленного первичного индекса.

- разогревающих операций: 3;
- измеряемых операций: 10;
- основная метрика: `build + flush`;
- загрузка исходных значений измеряется отдельно.

## А.7. Добавление и удаление

Для изменения до фиксации выполняются 200 разогревающих и 2000 измеряемых операций.

Для изменения с фиксацией:

- разогревающих пакетов: 2;
- измеряемых пакетов: 15;
- операций в пакете: 100.

SQLite-пакет включает transaction commit, WAL checkpoint и синхронизацию файлов; Polar.DB-пакет включает `Flush` и синхронизацию файлов. В таблице приводится время пакета, делённое на 100 операций.

## А.8. Исправленный эксперимент повторного открытия

Первый вариант SQLite `reopen-only` был отбракован после обнаружения включённого pooling.

Исправленный вариант:

- очищает существующие SQLite pools после подготовки набора;
- создаёт каждое измеряемое соединение с `Pooling=False`;
- выполняет 5 разогревающих и 30 измеряемых повторов;
- использует 50 000 и 5 000 000 записей;
- отдельно измеряет `open-only` и `query-ready reopen`;
- фиксирует native `sqlite_version()`.

Публикационная серия исправленного опыта:

`20260811T060237246Z-eed96558-reopen-only`.

Smoke-серия `20260811T060232899Z-eed96558-reopen-only`, содержащая только 5 измеряемых повторов на 50 тыс. записей, служила проверкой стенда и в статье не используется.

## А.9. Проверка корректности

Для lifecycle-операций заранее вычисляются число строк и контрольная сумма ожидаемого состояния. Для фаз поиска независимо вычисляются ожидаемые число строк и checksum для пакетного и latency-наборов ключей.

В исправленном reopen-опыте:

- 50 000 строк: checksum `982062805716110519`;
- 5 000 000 строк: checksum `1291763481180701370`.

Обе системы дали эти ожидаемые значения.

## А.10. Первичные результаты

Для каждого опыта стенд сохраняет:

- `combined.raw.json`;
- `manifest.json`;
- `sqlite.worker.json`;
- `polar-db.worker.json`;
- `samples.csv`.

Первичными для количественного анализа считаются JSON/CSV; HTML является производным представлением.

## А.11. Публикационная готовность

Запуск считается публикационным только при оптимизированной `Release`-сборке. Manifest фиксирует commit, признак чистого рабочего дерева, .NET runtime, архитектуру, Server GC, runtime overrides, версии assemblies и сведения о диске. После исправления стенда также фиксируется native SQLite version.

Основная серия и исправленная reopen-серия имеют `publicationReady = true`. Исправленная reopen-серия выполнена на чистом commit `eed965582a1f0b1e6d0796c09528f0b7876b1ba4`; GitHub CI и CodeQL для этого commit завершились успешно.

---

# Приложение Б. Технические сведения

## Б.1. Основная серия

- series id: `20260810T030729570Z`;
- commit: `e093da0247ec58c7fb78fc381eca52fa002b0967`;
- working tree: clean;
- Release;
- `.NET 10.0.7`, x64;
- Polar.DB assembly `2.1.3.0`;
- `Microsoft.Data.Sqlite` assembly `9.0.4.0`;
- Server GC: `false`.

## Б.2. Исправленная reopen-серия

- series id: `20260811T060237246Z-eed96558-reopen-only`;
- commit: `eed965582a1f0b1e6d0796c09528f0b7876b1ba4`;
- working tree: clean;
- Release;
- `publicationReady = true` у координатора и обоих worker-процессов;
- `.NET 10.0.7`, x64;
- SDK `10.0.203`;
- Polar.DB assembly `2.1.3.0`;
- `Microsoft.Data.Sqlite` assembly `9.0.4.0`;
- native SQLite `3.53.3`;
- engine order: Polar.DB → SQLite;
- `Pooling=False` для измеряемых SQLite reopen;
- пулы SQLite очищены после подготовки набора.

## Б.3. Операционная система и процессор

- Microsoft Windows 11 Pro, build `26200`;
- Intel Core i5-12400;
- 6 физических ядер;
- 12 логических процессоров;
- WMI L2 cache: 7680 KiB;
- WMI L3 cache: 18432 KiB.

## Б.4. Оперативная память

- 31,78 GiB видимой физической памяти;
- 4 × 8 GiB;
- configured clock speed: 3200 MT/s;
- две планки Patriot Memory `4400 C19 Series`;
- две планки Gloway `TAC4U3200E16081C`.

Конфигурация смешанная; значения WMI о nominal `Speed` различаются, поэтому в выводах не предполагается одинаковый SPD-профиль всех модулей.

## Б.5. Накопитель

- Samsung SSD 980 PRO 250GB;
- SSD, NVMe;
- GPT;
- рабочий том `D:` — NTFS.

## Б.6. `global.json`

На commit основной серии использовалось некорректное значение:

```json
{
  "sdk": {
    "version": "10.0.0",
    "rollForward": "latestMajor",
    "allowPrerelease": true
  }
}
```

Перед исправленной reopen-серией файл изменён на допустимую фиксацию SDK 10.0.203 с `latestPatch`. Поэтому прежняя серия остаётся историческим результатом с зафиксированным runtime, но без строгого SDK pin, а новая reopen-серия имеет воспроизводимую SDK-конфигурацию.

---

# Список литературы

1. Марчук А.Г. PolarDB — система создания специализированных NoSQL баз данных и СУБД // Моделирование и анализ информационных систем. 2014. Т. 21, № 6. С. 169–175.
2. Марчук А.Г. Архитектура и основные особенности библиотеки PolarDB работы со структурированными данными // Системная информатика. 2018. № 13. С. 25–34. DOI: 10.31144/si.2307-6410.2018.n13.p25-34.
3. Марчук А.Г. Последовательность как абстракция структурированного построения баз данных // Системная информатика. 2021. № 18. С. 35–52. DOI: 10.31144/si.2307-6410.2021.n18.p35-52.
4. Марчук А.Г., Лештаев С.В. Экспериментальная реализация Sparql-1.1 и RDF Triple Store // Аналитика и управление данными в областях с интенсивным использованием данных: труды XVII Международной конференции DAMDID/RCDL’2015. Обнинск, 13–16 октября 2015 г. С. 83–87.
5. Microsoft. Microsoft.Data.Sqlite: Connection strings. Документация параметров строки соединения, включая `Pooling`.

## Материалы воспроизводимости

Основная серия идентифицируется run id `20260810T030729570Z`. Исправленный опыт повторного открытия идентифицируется run id `20260811T060237246Z-eed96558-reopen-only`. Для последнего сохранены `combined.raw.json`, `manifest.json`, оба worker JSON и `samples.csv`; отдельное описание проверки приведено в `benchmarks/docs/REOPEN_RERUN.md`.

Настоящий черновик намеренно не содержит утверждения об общем превосходстве Polar.DB над SQLite. Все коэффициенты относятся только к явно указанным операциям, размерам данных, версиям программ и состоянию хранилища.