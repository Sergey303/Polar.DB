# Stage 1 Scope

## Included now

- shared DTO and contract layer;
- result file naming and environment collection helpers;
- executor shell with a working synthetic adapter;
- analyzer with baseline/policy evaluation support;
- charts project that emits markdown and CSV summaries;
- adapter skeletons for Polar.DB and SQLite;
- sample docs, policy, baseline, and experiment files.

## Intentionally deferred

- real Polar.DB data-path implementation;
- real SQLite data-path implementation;
- full chart rendering;
- broad experiment matrix;
- full CI wiring;
- historical baseline update workflow.
