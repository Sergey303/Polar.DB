# Contracts Folder

This folder stores policy contracts for analysis.

Stage 1 includes one sample file:

- `sample-policy.v1.json`

Policies are versioned separately from raw results so historical runs can be re-analyzed without rerunning benchmarks.
